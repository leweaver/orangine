#pragma once

namespace oe {
    enum class Entity_filter_mode {
        // All of the filter conditions must be met
        All,
        // Any one of the filter conditions must be met
        Any
    };
}
