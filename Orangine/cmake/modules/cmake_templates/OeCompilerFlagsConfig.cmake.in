# Compute paths
get_filename_component(OECOMPILERFLAGS_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(OECOMPILERFLAGS_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

# Our library dependencies (contains definitions for IMPORTED targets)
include("${OECOMPILERFLAGS_CMAKE_DIR}/OeCompilerFlagsTargets.cmake")
